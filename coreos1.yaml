ignition:
  config:
    append:
      - source: "https://github.com/Dev-Time/container-linux-config/raw/master/common.json"
storage:
  files:
    - path: "/etc/hostname"
      filesystem: "root"
      mode: 420
      contents:
        inline: "coreos1"